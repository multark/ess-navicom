import type { tState } from '../../../src/components/Accordion/types';
export declare function useAccordionState(id?: string, options?: {
    flush: boolean;
    alwaysOpen: boolean;
}): {
    accordionsStates: tState;
};
